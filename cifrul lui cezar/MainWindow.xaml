<Window x:Class="cifrul_lui_cezar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cifrul_lui_cezar"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Proiect: Cifrul lui Cezar (+3)" 
                   FontSize="22" FontWeight="Bold" Foreground="#2C3E50" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <TextBlock Grid.Row="1" Text="Introduceți textul aici:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtCezarInput" Grid.Row="2" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="10"
                 BorderBrush="#BDC3C7" BorderThickness="1"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
                <Button Content="CRIPTEAZĂ" Click="BtnCriptare_Click" Width="110" Height="35" 
                    Margin="5" Background="#3498DB" Foreground="White" FontWeight="Bold"/>

                <Button Content="DECRIPTEAZĂ" Click="BtnDecriptare_Click" Width="110" Height="35" 
                    Margin="5" Background="#2ECC71" Foreground="White" FontWeight="Bold"/>

                <Button Content="CRIPTANALIZĂ" Click="BtnAnaliza_Click" Width="110" Height="35" 
                    Margin="5" Background="#E74C3C" Foreground="White" FontWeight="Bold"/>
            </StackPanel>

            <TextBlock Grid.Row="4" Text="Rezultat:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtCezarResult" Grid.Row="5" IsReadOnly="True" 
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="10"
                 Background="#ECF0F1" BorderBrush="#BDC3C7" BorderThickness="1"/>
   </Grid>
</Window>
